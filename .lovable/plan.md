

## План: сделать все формы бронирования реальными (редирект в MAX)

Сейчас «Отправить заявку» показывает только toast. Нужно: при отправке любой формы бронирования формировать готовое сообщение со всеми данными и открывать MAX.

### Что меняется

**1. `src/components/BookingForm.tsx`**
- `handleSubmit`: вместо toast → собрать текст сообщения (даты, гости, доп. место, имя, телефон, комментарий) и открыть `window.open(buildMaxUrl())`. Показать toast «Открываем MAX для подтверждения брони...»
- `buildMaxUrl()`: добавить имя, телефон и комментарий в текст сообщения
- Убрать кнопку «Написать в MAX» (дублирует), оставить одну кнопку «Забронировать в MAX»

**2. `src/components/HeroSection.tsx`**
- Hero booking bar: добавить state для дат заезда/выезда
- Кнопка «Забронировать»: вместо scroll → открыть MAX с предзаполненными датами
- Если даты не выбраны → scroll к секции `#booking`

**3. `src/components/StickyBookingBar.tsx`**
- Кнопка: открывает MAX с базовым текстом бронирования (без дат, т.к. в sticky bar их нет)

### Файлы:
- `src/components/BookingForm.tsx` — переделать submit
- `src/components/HeroSection.tsx` — добавить state дат + редирект
- `src/components/StickyBookingBar.tsx` — редирект в MAX




## План: обновить блок «Условия бронирования» с grid-секцией

Заменить текущий простой блок `BookingTerms` на расширенную секцию с сеткой карточек, используя данные из предоставленного текста. Компонент `feature-section-with-grid` адаптируем под контент базы отдыха — не копируем как есть, а используем его layout-паттерн (badge + заголовок + grid 2×3) прямо в `BookingTerms.tsx`.

### Контент карточек (6 штук):

1. **Заезд и выезд** — Заезд с 14:00, выезд до 12:00
2. **Оплата** — Наличные и банковская карта. Гарантийный залог при заезде
3. **Предоплата** — Бронь подтверждается предоплатой в размере одних суток
4. **Отмена брони** — Возврат предоплаты за 30 дней до даты заезда
5. **Размещение** — Паспорт при заселении, соблюдение тишины, некурящие номера
6. **Животные** — Размещение с животными по согласованию

### Что сделать:

- **`src/components/BookingTerms.tsx`** — переписать: добавить Badge «Бронирование», grid 2×3 с иконками из lucide-react (`Clock`, `CreditCard`, `Banknote`, `CalendarX`, `UserCheck`, `PawPrint`), сохранить блок CTA с MAX внизу
- **`src/components/ui/feature-section-with-grid.tsx`** — НЕ создавать отдельный файл; паттерн интегрируем напрямую в BookingTerms, чтобы не плодить неиспользуемые компоненты
- Badge уже есть в проекте — зависимости установлены

### Файлы:
- `src/components/BookingTerms.tsx` — полная переработка

